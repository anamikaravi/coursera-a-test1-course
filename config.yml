title: coursera-a-test1
description: coursera test repository
template:
  repo: coursera-a-test1-template
  name: coursera-a-test1
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
